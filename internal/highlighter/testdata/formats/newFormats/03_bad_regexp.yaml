formats:
  test:
    - regexp: 'd{1,3}(\.\d{1,3}){3}'
      name: one
    - regexp: '[^ ]+'
      name: two

themes:
  test:
    formats:
      test:
        one:
          fg: "#f5ce42"
        two:
          bg: "#764a9e"
